/**
 * Production environment. The web app is expected to be served from the same
 * origin as the API (reverse proxy), so relative URLs are used.
 */
export const environment = {
  production: true,
  apiUrl: '/api',
};
